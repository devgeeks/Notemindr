'use strict';

var React = require('react');

require('./index.less');

var Header = React.createClass({

  render: function() {
    return (
      <header className='header' />
    );
  }
});

module.exports = Header;
